/* One size for every icon on the site. Lucide draws on a 24px grid, so a
   single class plus a single stroke keeps arrows, chevrons and type marks
   optically equal wherever they land. */
export const ICON = "h-4 w-4 shrink-0";
export const ICON_STROKE = 2.2;
